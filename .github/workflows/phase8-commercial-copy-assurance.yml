name: Phase 8 Commercial Copy Assurance

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'index.html'
      - 'pricing.html'
      - 'beta-terms.html'
      - 'terms.html'
      - 'refund.html'
      - 'assets/js/marketing-v3.js'
      - 'scripts/validate-phase8-commercial-copy.mjs'
      - 'scripts/validate-homepage-information-architecture.mjs'
      - '.github/workflows/phase8-commercial-copy-assurance.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate homepage, pricing, beta, subscription, and refund copy
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Parse validators
        run: |
          node --check scripts/validate-phase8-commercial-copy.mjs
          node --check scripts/validate-homepage-information-architecture.mjs
      - name: Validate locked commercial copy
        run: node scripts/validate-phase8-commercial-copy.mjs
      - name: Validate concise homepage information architecture
        run: node scripts/validate-homepage-information-architecture.mjs
